schema: `${process.env.NEXT_PUBLIC_GRAPHQL_ENDPOINT}/api/graphql`
documents: './src/**/*.graphql'
generates:
  ./src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request